import { simpleArraySum, prime, stairCase, minMax } from "./Easy/index.js";

// simpleArraySum();

// console.log(prime.primeArray);

// stairCase(10);

const { minSum, maxSum } = minMax([1, 2, 3, 4, 5]);

console.log(minSum, maxSum);
